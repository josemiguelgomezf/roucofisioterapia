# Copia este bloque dentro de tu server {} en nginx.conf
location / {
    try_files $uri $uri/ /index.html;
}

# Para devolver 404 HTTP real en rutas inexistentes necesitarías
# pre-rendering o SSR. Con SPA pura, el servidor siempre sirve
# index.html con 200 y React Router gestiona la vista 404.
